import 'package:flutter/material.dart';

class LandingPagePlaceholder extends StatelessWidget {
  const LandingPagePlaceholder({super.key});
  @override
  Widget build(BuildContext context) => const Scaffold(body: Center(child: Text('Landing Page')));
}

class LoginPlaceholder extends StatelessWidget {
  const LoginPlaceholder({super.key});
  @override
  Widget build(BuildContext context) => const Scaffold(body: Center(child: Text('Login Page')));
}

class RegisterPlaceholder extends StatelessWidget {
  const RegisterPlaceholder({super.key});
  @override
  Widget build(BuildContext context) => const Scaffold(body: Center(child: Text('Register Page')));
}

class VerifyEmailPlaceholder extends StatelessWidget {
  const VerifyEmailPlaceholder({super.key});
  @override
  Widget build(BuildContext context) => const Scaffold(body: Center(child: Text('Verify Email Page')));
}

class ForgotPasswordPlaceholder extends StatelessWidget {
  const ForgotPasswordPlaceholder({super.key});
  @override
  Widget build(BuildContext context) => const Scaffold(body: Center(child: Text('Forgot Password Page')));
}
